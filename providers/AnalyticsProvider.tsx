"use client";
// AnalyticsProvider — placeholder de infraestructura (sección 5.4: providers/).
// Sin lógica de producto todavía.
export function AnalyticsProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
