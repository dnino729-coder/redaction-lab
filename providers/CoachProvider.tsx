"use client";
// CoachProvider — placeholder de infraestructura (sección 5.4: providers/).
// Sin lógica de producto todavía.
export function CoachProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
