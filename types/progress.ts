// Tipos compartidos de "progress" (sección 5.4). Sin definición de producto todavía;
// se completa en coherencia con el modelo de datos (sección 13) al desarrollar el módulo.
export type ProgressPlaceholder = Record<string, never>;
