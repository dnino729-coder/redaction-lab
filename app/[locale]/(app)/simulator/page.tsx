// Ruta: Simulador DELF B2 (sección 6.9). Sin lógica de producto en app/
// (sección 5.4) — solo importa y renderiza la superficie pública de la
// feature (features/simulator/pages), mismo patrón que
// app/[locale]/(app)/daily-training/page.tsx.
import SimulatorPage from "@/features/simulator/pages";

export default SimulatorPage;
